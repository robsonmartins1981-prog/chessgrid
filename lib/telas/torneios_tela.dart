import 'package:flutter/material.dart';

class TorneiosTela extends StatelessWidget {
  const TorneiosTela({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Esta é a tela de Torneios.',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}